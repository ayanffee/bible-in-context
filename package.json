{
  "name": "bible-in-context",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "concurrently \"node --watch server/src/index.js\" \"npm run dev --prefix client\"",
    "start": "node server/src/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.54.0",
    "better-sqlite3": "^12.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
